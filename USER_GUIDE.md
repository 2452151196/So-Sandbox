# SO Sandbox 使用说明

## 功能介绍

SO Sandbox 是一款 Android 平台的 SO 文件分析工具，支持查看函数列表、反汇编、字符串表、PLT 表、交叉引用、JNI RegisterNatives 分析等功能。

---

## 使用方法

### 1. 加载 SO 文件

点击主界面的 **"打开 SO 文件"** 按钮，选择要分析的 SO 文件。

加载时会询问模式：
- **动态加载**：加载到内存，可获取基址
- **仅静态分析**：不加载，仅解析文件内容

### 2. 查看函数列表

加载成功后，点击 **"查看函数"** 按钮。

- 顶部搜索框可按函数名过滤
- 点击右上角「更多」→「排序方式」可排序
- 点击函数进入详情
- 长按函数查看交叉引用

### 3. 查看字符串表

点击主界面的 **"查看字符串"** 按钮。

- 搜索框可过滤字符串
- 点击右上角漏斗图标过滤 C++ 符号表字符串
- 点击字符串查看引用它的函数
- 长按复制字符串

### 4. 查看 PLT 表

点击主界面的 **"查看 PLT"** 按钮，查看外部依赖函数。

### 5. JNI RegisterNatives 分析

点击主界面的 **"RegisterNatives 代理"** 按钮。

- **静态模式**：查看 `Java_` 前缀的静态注册函数
- **动态模式**（需授权）：Hook JNI_OnLoad 捕获动态注册的函数

点击函数的 **"调用"** 按钮可执行该函数。

### 6. 函数详情

点击任意函数进入详情页面，包含以下标签：

- **汇编**：查看 ARM64 汇编代码，点击函数名可跳转，点击指令可编辑
- **伪 C**：查看反编译的类 C 代码
- **Hex**：查看和编辑机器码
- **Sections**：查看 ELF Section 信息
- **流程图**（需授权）：生成控制流图

### 7. 设置

点击右上角菜单 → 设置

- 切换主题（跟随系统/浅色/深色）
- 调整代码字体大小
- 激活卡密（用于高级功能）
- 查看版本和更新检测

---

## 权限说明

- Android 11+：需要"全部文件访问权限"
- Android 6-10：需要存储权限
- Android 13+：需要媒体权限

---

## 注意事项

- 仅支持 ARM64 架构的 SO 文件
- 修改 SO 文件会直接写入，请先备份
- 流程图、动态注册等功能需要云端授权

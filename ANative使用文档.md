# ANative — Android SO 逆向分析工具

## 简介

ANative 是一款运行在 Android 设备上的本地 SO (Shared Object / Native Library) 逆向分析工具，基于 ARM64 架构开发。它集成了反汇编、反编译、交叉引用分析、HEX 编辑、函数追踪、动态调用等多项功能，支持动态加载与纯静态分析两种工作模式。

---

## 功能特点

### 1. 双模式 SO 加载

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| **动态加载** | 调用 `dlopen` 加载 SO，从 `/proc/self/maps` 获取基址 | 需要运行时调用函数 |
| **静态分析** | 仅复制文件不加载到内存，基址为 0 | SO 存在未满足的依赖、无法动态加载时 |

用户可在首页切换模式，工具会自动处理权限和 URI 持久化。

### 2. 全面的函数解析

- 遍历 ELF 的 `SYMTAB` / `DYNSYM` 提取函数符号
- 从 `.init_array` / `.fini_array` 补充函数
- 对 PIE SO 的 init_array 通过重定位表解析真实值
- 对大小为 0 的函数，使用 **CFG (控制流图)** 分析确定边界
- 对 `sub_XXXX` 这类合成函数进行去重和范围重叠检测
- 自动推断函数签名（返回值类型、参数列表）
- 自动 demangle C++ 符号名
- 自动识别 JNI 函数（`Java_*` 前缀）

### 3. 交叉引用分析 (XRef)

双重实现，适用于不同场景：

- **XRefAnalyzer**（运行时）：对单函数执行 BL/B 指令分析，返回 `caller|callee|type` 格式结果，构建 incoming/outgoing 引用关系图
- **XRefScanner**（静态批量）：一次性扫描整个 `.text` 段所有 BL/B 指令，匹配已知函数地址表，同时构建**函数调用图**和**字符串引用图**

长按任意函数即可触发批量扫描，结果缓存避免重复工作。

### 4. 多维度函数详情分析

函数详情页面提供 5 个分析 Tab：

| Tab | 功能描述 |
|-----|----------|
| **汇编 (AsmFragment)** | Capstone 反汇编引擎，带函数名/字符串/PLT 注释，点击可编辑单条指令，函数调用可跳转 |
| **伪C (PseudoCFragment)** | 本地伪C反编译引擎，集成 AI 增强选项（GPT/Gemini），支持 OpenAI 和 Gemini 两种响应格式 |
| **HEX (HexFragment)** | 字节级编辑器，点击单个字节修改，直接写入 SO 文件 |
| **Sections (TextFragment)** | 查看该函数所在段的详细信息 |
| **流程图 (FlowChartFragment)** | BasicBlock 控制流图可视化 |

### 5. 指令级交互 (AsmFragment)

在汇编视图中支持丰富的交互操作：

- **点击函数名** → 跳转到目标函数
- **点击偏移地址** → 复制地址
- **点击寄存器** → 展开寄存器历史值
- **点击立即数** → 显示数值进制转换
- **点击编辑按钮** → 输入新指令，Keystone 引擎汇编写入 SO 文件
- **点击 HEX 按钮** → 展开 HEX 编辑模式

### 6. AI 增强伪C (PseudoCFragment)

- **本地伪C**：基于反汇编的 CFG 分析，生成结构化伪代码
- **AI 增强**：支持配置 OpenAI GPT 和 Google Gemini API，可选是否追加 AI 解释说明
- 自动解析 API 响应，自动检测格式（`gpt` 或 `gemini` 格式）

### 7. 动态函数调用

通过 libffi 实现通用函数调用：

- **libffi 调用**：优先使用 libffi 通用调用引擎，支持所有参数类型
- **Fallback 调用**：退化策略为直接函数指针调用（x0-x7 传参）
- 所有调用操作被 `sigsetjmp` / `siglongjmp` 崩溃保护包裹，不会导致 App 崩溃
- 支持自定义 JNI 调用参数（`JNIEnv*`、对象、方法ID）

### 8. JNI 函数注册表追踪

- Hook `RegisterNatives`，捕获所有动态注册的 JNI 函数
- Hook `FindClass`，防止第三方 SO 崩溃
- 自动触发 `JNI_OnLoad`，触发时自动注入 App ClassLoader
- 动态注册的函数支持**直接调用**（带参数编辑对话框和追踪日志）
- 静态注册的函数（`Java_*` 开头）以只读方式展示

### 9. 函数执行追踪

- 对指定函数启用追踪
- 记录执行过程中的所有行为日志
- 支持获取和清除追踪日志

### 10. SO 文件直接修改

- **HEX 编辑**：以字节为单位直接修改 SO 文件
- **汇编写入**：使用 Keystone 引擎将新指令汇编后写入
- 所有修改通过 `FileOutputStream` 直接刷新到磁盘
- 支持分享修改后的 SO 文件

---

## 使用方法

### 首次使用

1. **安装并启动** ANative App
2. App 会请求存储权限，授予后即可开始使用
3. 首次打开会显示空白的首页，点击 **"加载 SO 文件"** 进入文件选择器

### 加载 SO 文件

1. 点击 **"加载 SO 文件"**
2. 从设备中选择目标 SO 文件（通常在 `/data/app/<package>/lib/arm64/` 目录下）
3. 选择加载模式：
   - **动态加载** — 推荐，启用完整功能（动态调用、追踪等）
   - **仅静态分析** — SO 无法加载时使用（功能受限）
4. 等待解析完成，工具会自动提取函数列表并展示

### 浏览函数列表

1. 函数列表按地址排序，支持以下排序方式：
   - 按函数起始地址
   - 按函数大小
2. **搜索栏** 实时过滤函数名
3. **点击** 任意函数 → 进入函数详情页（5个Tab）
4. **长按** 任意函数 → 触发交叉引用扫描 → 弹出 XRef 对话框

### 函数详情分析

在函数详情页，各 Tab 的操作：

**汇编 Tab**
- 上下滑动浏览反汇编代码
- 点击行内元素进行跳转或编辑
- 编辑后点击保存按钮写入 SO 文件

**伪C Tab**
- 向上滑动查看本地伪C代码
- 如需 AI 增强：点击 AI 设置按钮 → 配置 API Key 和模型 → 选择 AI 类型 → 生成

**HEX Tab**
- 点击任意字节 → 输入新值 → 自动写入 SO 文件

**流程图 Tab**
- 自动绘制 BasicBlock 控制流图
- 节点表示基本块，边表示控制流跳转

### 调用函数

1. 从首页点击 **"动态调用"** 或 **"JNI 函数表"**
2. 在动态调用界面：
   - 选择要调用的函数
   - 编辑参数类型和值
   - 点击 **"调用"**
3. 在 JNI 函数表界面：
   - 动态注册的函数有 **"调用"** 按钮
   - 点击后可编辑参数并执行追踪

### 修改 SO 文件

**HEX 修改**
1. 进入任意函数的 HEX Tab
2. 点击目标字节
3. 输入新的十六进制值
4. 自动保存到 SO 文件

**汇编修改**
1. 进入任意函数的汇编 Tab
2. 点击目标指令旁的 **编辑按钮**
3. 输入新的汇编指令
4. 点击确认，Keystone 引擎汇编后写入

### 设置

点击右上角菜单 → **设置**，可配置：

- **主题切换**：跟随系统 / 浅色 / 深色
- **反汇编选项**：注释级别、地址格式
- **默认加载模式**：动态加载 / 仅静态分析
- **AI API 设置**：API Key、模型选择、AI 类型（GPT/Gemini）
- **最近项目**：查看和管理历史加载的 SO 文件

---

## 技术架构

```
┌─────────────────────────────────────────────┐
│                   UI 层                      │
│  MainActivity → FunctionListActivity        │
│  → FunctionDetailActivity (5-Tab Pager)      │
│  → RegisterNativesActivity / SettingsActivity│
├─────────────────────────────────────────────┤
│                业务层 (Java)                  │
│  DataHolder ──── 跨Activity数据总线           │
│  SoLoader ────── SO加载与文件管理             │
│  ElfParser ───── ELF文件解析                  │
│  XRefScanner ─── 静态XRef批量扫描             │
│  XRefAnalyzer ── 运行时XRef分析               │
│  NativeInvoker ─ JNI方法声明（30+个native方法）│
├─────────────────────────────────────────────┤
│                Native层 (C)                   │
│  invoker.c (≈2600行)                         │
│  ├─ Capstone 反汇编引擎                       │
│  ├─ Keystone 汇编引擎                         │
│  ├─ libffi 动态调用                          │
│  ├─ 反编译器 (伪C生成)                        │
│  ├─ 崩溃保护 (sigsetjmp/siglongjmp)          │
│  └─ XRef批量扫描实现                          │
├─────────────────────────────────────────────┤
│              外部依赖库                        │
│  net.fornwall:jelf — ELF文件解析             │
│  Capstone — ARM64 反汇编                     │
│  libffi — 通用动态调用                       │
│  Keystone — 指令汇编                          │
└─────────────────────────────────────────────┘
```

### 核心设计原则

- **DataHolder 单例**：避免 Intent 传大数据，跨 Activity 共享函数列表、字符串表等
- **崩溃保护**：所有内存操作和函数调用被信号处理包装，保证 App 稳定性
- **按需加载**：函数列表解析完成后才加载详情，XRef 扫描结果缓存
- **双模式兼容**：同一 SO 可随时在动态/静态模式间切换分析
- **直接修改**：HEX 编辑和汇编写入直接操作 SO 文件，无需重新打包
